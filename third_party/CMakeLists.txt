# Create an INTERFACE library target named 'range-v3'
#add_library(range-v3 INTERFACE)

# Specify the include directories for the 'range-v3' target.
#target_include_directories(range-v3 INTERFACE
        # Add the 'include' subdirectory relative to the source directory
#        ${CMAKE_CURRENT_SOURCE_DIR}/range-v3/include)